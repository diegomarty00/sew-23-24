Usuario que utilizar en XAMP
	DBUSER2023 / DBPSWD2023 --> Con todos los permisos (No es recomendable pero lo usaremos)


Tabla --> Guardar un archivo para que puedan crear la misma base de datos para corregir
	Nombre = String
	Tiempo = int (segundos) --> Usar conversiones de segundos a minutos si es necesario para mostrar resultados


PHP se ejecuta directamente en el servidor, como se delega al servidor, no se puede parar ni ejecutar el inspector con el php
Si queremos saber los valores, usamos el print.r($vale) o el var.dump($vale) exit; para parar la ejecución.
	crucigrama.html --> crucigrama.php (Nos devuelve la salida de la ejecucón, que es un HTML.


Meter metodos php con llaves
<?php

?>


Se puede tambien usar una llamar el valor de una ejeción
<p><?=$v->metodo()</p>



La parte del codigo que está en azul, es la que el código se ejecuta en el servidor, y el resultado del codigo, es justo lo que está debajo,
que es el resultado del código de arriba. Solo se vería sino los valores del servidor, y no podemos entrar en ella.